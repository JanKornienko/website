{block description}Project portfolio of Jan Kornienko.{/block}
{block keywords}Portfolio, Projects, GitHub, Download{/block}
{block og-title}Project Portfolio | Jan Kornienko{/block}
{block og-url}https://jankornienko.com/projects/{/block}
{block content}
	<!-- Projects Section -->
	<section class="section">
		<div class="container">
			<h1 n:block=title class="title has-text-centered">Projects</h1>
			<div class="columns is-multiline is-mobile is-centered">
				{foreach $projects as $project}
					<div class="column is-narrow" data-id="{$project->id}">
						<div class="box">
							<h2 class="subtitle">{$project->name}</h2>
							<a class="button is-dark is-outlined" href="{$project->link}" target="_blank" rel="noopener noreferrer">GitHub</a>
							<a class="button is-dark is-outlined" n:href="Projects:detail $project->id">Detail</a>
							<a class="button is-danger is-outlined" n:href="Projects:deleteProject $project->id" n:if="$user->isLoggedIn()">Delete</a>
						</div>
					</div>
				{/foreach}
			</div>
		</div>
	</section>
{/block}